<!-- Page Content -->
   <!--  <div class="container"> -->

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Dog Advertisements
                    <!-- <small>Secondary Text</small> -->
                </h1>
            </div>
        </div>
        <!-- /.row -->
            
        <% @ads.each do |ad| %>
            <div class="col-xs-6 col-md-3 portfolio-item">
                <h4> <%= link_to ad.title, ad %> </h4>
                
                <p> 
                <%= truncate(ad.description, :length => 50) %> 
                <% if(ad.description.length > 50) %>
                      <%= link_to 'Read More', ad %>
                <% end %>
                </p>

<<<<<<< HEAD
                <%= image_tag(ad.image, size:"200") %>
                <!-- <img class="img-responsive" src=ad.image alt=""> -->
                
                
            </div>
        <% end %>

        <%= link_to 'New Ad', new_ad_path %>
=======
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Price</th>
      <th>Description</th>
      <th>Image</th>
      <th>User</th>
      <th>Category Number</th>
      <th>Category </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ads.each do |ad| %>
      <tr>
        <td><%= ad.title %></td>
        <td><%= ad.price %></td>
        <td><%= ad.description %></td>
        <td><%= ad.image %></td>
        <td><%= ad.user_id %></td>
        <td><%= ad.category_id %></td>
        <td><%= ad.category.name %></td>
        <td><%= image_tag ad.image.url(:thumb) %></td>
        <td><%= link_to 'Show', ad %></td>
        <td><%= link_to 'Edit', edit_ad_path(ad) %></td>
        <td><%= link_to 'Destroy', ad, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ad', new_ad_path %>
>>>>>>> 54338af3d72659d05cc2be34b3af7426e81c488b
